using System.Collections;
using System.Collections.Generic;
using UnityEngine;

/// <summary>
/// Represents the charge of an object. Cast to an int to get the numeric version.
/// </summary>

public enum Charge
{
   Neutral = 0,
   Positive = 1,
   Negative = -1,
}
